## 16:20 작업
**요청:** CodeCanvas에서 코드 작성/실행 후 결과물을 연결된 ClassLog 교실에 바로 제출할 수 있는 버튼 추가
**작업 내용:**
- vibe-coding-studio → CodeCanvas 진입 시 entryCode/studentName을 URL 쿼리로 전달 (bridge.js `sendToCodeCanvas()` 수정)
- CodeCanvas `js/app.js`에서 URL 쿼리 캡처 → localStorage 저장 → 제출 버튼 노출 여부 결정
- `index.html`에 "클래스에 제출" 버튼 추가 (entryCode 있을 때만 노출)
- 신규 `CodeCanvas/api/submit.js` 프록시 추가 (ESM Fetch API 스타일, `ai-assistant.js`와 동일 패턴) — AIServiceHub `/api/classlog/submission`으로 전달
- 인증은 별도 키 `CODECANVAS_API_KEY` 신규 발급 (STUDIO_API_KEY와 분리, 보안 경계 유지)
- `ai-service-site/app/api/classlog/submission/route.ts`의 `isAuthorized()`가 STUDIO_API_KEY 또는 CODECANVAS_API_KEY 둘 다 허용하도록 수정
- 제출 payload: `EditorManager.getCode()`의 html/css/js를 하나의 텍스트로 합쳐 `resultType: 'text'`로 제출
